预期进行一轮功能的扩展优化，接下来我会分APP端、网页端和两端共有的来阐述，如果有任何的问题，请先问清楚了，再进行下一步，另外，由于改动面比较大，我建议你整理出所有需要改动和牵涉到的点，然后再一一改进
双端：
一、优化缓存机制
Web：
一、新增超级导入导出功能
1概述：将班级数据导出为zip（或其他更适合的格式），并支持在选择/创建班级界面导入此格式文件，以格式文件中的数据为模板创建新班级
2.位置及UI：
2.1 导出：在班级的功能选择页新增一个功能卡片叫做超级导出，点击进入之后 选择需要导出的数据（知识库，班级图集，默写记录，班级史记等等，尽量分细一点，但是需要注意的是直接把班级名称包含在内，另外这边做一个取舍 就不支持导出班级内用户产生的隐私数据（如用户名、密码、个人vlog（ 不论用户有没有授权个人vlog的查看），因为这毕竟是高危操作），勾选完毕之后导出，并让用户可以下载导出的文件
2.2 导入：在新建/选择班级页新增一个导入键，用户点击后提示用户选择文件导入，并且提示用户并没有附带APP用户信息，那么就需要重新注册账号等
2.注意事项：一定注意安全性的把握
二、单词库功能拓展升级
1概述：由于需求的升级，单词库不再局限于存储单词，还要存储一些需要默写的句子和作文，所以我们将原先的单词库改为知识库，并且对知识库的UI进行一些调整保证人性化和可用性
2.位置及UI（知识库界面）：
2.1 计划在知识库顶部设计双导航栏逻辑
2.11 第一层导航栏与相较于之前少了跟读键，然后将创建默写任务、导入/导出CSV移到这里（那么就要注意这个空间的布局，尤其是对于公告的协调，建议将文字按钮改为SVG图标按钮 节省空间）
2.12 第二层导航栏用来放一个切换的标签tab栏，在这里单词、句子或者是作文界面进行选择，并且在每个标签后面跟上对应选择的个数（比如说我选了20个单词，两个句子和一篇作文，那么显示的应该是在单词后面跟一个数字20，在句子后面跟一个数字2，在作文后面跟一个数字1）。并且仍然沿用定位逻辑，刚进来，因为展示的是单词页，那么我们肯定就定位单词。用户切换到句子页或者作文页的话，我们也同样定位到上一次默写的位置
2.2 切换到单词库页面的时候，二级导航栏增加一个跟读键，然后就照常逻辑跟读单词
2.3 切换到句子页面的时候，就显示句子的中文意思和英文，具体怎么布局看你，我觉得可以就一排一句（与单词的处理逻辑一样，如果说空间大了，那么文字就居中，如果说空间不够，那么文字就滚动跑马灯显示）
2.4 切换到作文页面的时候，每个作文的卡片里面就显示标题（如果有），下面跟具体的作文内容，你看怎么处理，是全部展示完还是说展示前一段，但是一行不展示多个作文
2.5 添加键一直悬浮在右下角，跟随着标签页的切换而改变。
2.51 如果说正处在单词这一页，那么就和以前的一样。
2.52 如果说在句子那一页 那么就要修改一下布局，我觉得可以让手动输入英文 然后AI补全释义（注意这里的提示词让AI直译，尽量不要意译）。
2.53 如果处在作文页，那么这个时候我们就是要输入作文英文以及标题（如果有），然后AI直译补全释义
3.位置及UI（任务界面）
3.1 整体上变化不大还是分展示、默写和听写三个，但是需要注意的是 如果说混合了有句子或者说作文的话 那么我们就不支持听写
3.11 展示页面就和在知识库中的差不多，把它们拼接在一起就行
3.12 默写页面就是展示中文意思，但是需要注意的是，考虑到方便性，如果要默写的是句子或者作文的话，这里的中文释义我们就暂时不做一个跑马灯显示，就扩宽视口高度，让中文尽量放得下，并且呢，如果说三者同时存在，那么作文在最后，句子在倒数第二个，单词是在最前面默写。另外，句子和作文在同一行内都不展示多个，就一行一个
3.13 听写页面没变化，但是注意，如果说混合了句子或者作文的话，那么就没有这个听写页面，也就注意一下切换栏里面的改变
4.注意事项：句子与作文不受数量限制，创建默写任务的时候是所有选中的一起进行一个创建，创建的逻辑与之前基本相同，在创建周末大礼包的时候，我们的句子和作文是不参与进来的。并且因为这个改变了，整个改动面就比较大，包括也要修改默写记录界面，搜索、定位逻辑等等
三、
默写时整体字号变大一点 但是注意也不能溢出
四、
支持自定义AI API，在每个班的班级设置页里面改变，就让用户选择是Anthropic端点格式还是OpenAI端点格式，然后要求用户填写接口地址和API密钥。这个具体的UI和逻辑怎么做看你
APP
真正独立的改动就一个，其他的就是跟随着网站端的改动而改动
一、自定义请求端点
1.概述：用户首次打开APP时，提醒用户自己选择API请求端点（注意区分HTTP和HTTPS），以后每次打开APP就默认进入这个端点，如果发现不可用的话，就提醒用户服务器开机或者说是更换端点地址
2.注意事项：对于具体逻辑的处理问题
二、牵涉改动
比如搜索、展示和定位逻辑等等，我这里挑几点重要的来说，其他的你可以看着办，也可以问我
1.句子的呈现形式：以卡片形式呈现，上面是句子，下面是中文释义，这里同样以跑马灯形式，并且支持用户自己手动拖动
2.作文的呈现形式：展示标题和部分内容（英文），用户点击卡片进去之后可以展示详细内容
3.展示的取舍：把单词、句子和作文放在一个页面下展示，因为也放不下切换页了，把它们放在原来的单词库页里面，先展示单词 再展示句子 最后展示作文
4.错题本：支持添加句子和作文
5.注意事项：一定注意搜索，刷新，定位逻辑的优化，因为之前踩过很多次坑，就是搜索之后各种定位失败，定位超时（所以我建议用户在搜索那里搜索到之后，我们加一个正在定位的 loading 提示）。并且之前有案例 手动刷新了一段时间就滑不动了

其他重要的：
一、以后 commit 时让远端仓库带上 APP 版本更新的日志，也就是我们主页面的那个下载APP的版本记录
二、目前我已将这个项目在GitHub上开源，变成一个公开的，所以对应的你可能需要做一些处理
三、服务器已经下线了，那么就需要你同步更新一下各种README说明文档


